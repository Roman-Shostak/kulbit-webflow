# Kulbit — Архітектурні рішення (ADR)

> Винесено з CLAUDE.md (§10) для зменшення основного контексту. Це ПОВНИЙ архів рішень із деталями механіки.
> CLAUDE.md тримає лише короткий індекс ADR — за деталями конкретного рішення читай цей файл.

---


Короткий список ключових рішень та чому вони такі.

### ADR-001: Відмова від fullpage.js

**Рішення:** не використовувати fullpage.js, побудувати snap-логіку на GSAP Observer.
**Причини:**

- Економія $30-80 на ліцензіях (core + extensions)
- Гнучкість для **>3 секцій з покроковою анімацією** (у fullpage це хак через `onLeave + return false`, в Observer — нативна архітектура)
- Жодних обмежень по доменах і activation keys
- Stacking-ефекти (секція 2 наїжджає на секцію 1) — через GSAP-таймлайни на події Observer (⚠️ оновлено ADR-008: НЕ через ScrollTrigger pin, бо вільного скролу немає)
- Roman прокачує реальні JS-навички

### ADR-002: ScrollSmoother замість Lenis — ⚠️ СКАСОВАНО (див. ADR-008)

> **Статус: скасовано на Кроці 3 (21.05.2026).** ScrollSmoother прибрано повністю — він призначений для плавного *вільного* скролу, що прямо конфліктує з нашим жорстким snap (немає вільного скролу взагалі). Замість нього — рух треку трансформом + Observer. Деталі в ADR-008. Текст нижче лишаємо для історії.

**Рішення (історичне):** ScrollSmoother від GreenSock, не Lenis.
**Причини:**

- Нативна інтеграція з ScrollTrigger (одна команда створення)
- Безкоштовний у Webflow з 2025 (партнерство з GreenSock)
- Вбудовані `data-speed` / `data-lag` атрибути для parallax-ефектів
- Одна підтримка всього стеку (GreenSock)

### ADR-003: Гібридне рішення для кнопок-переходів

**Рішення:** кнопки ведуть на секцію + крок з авто-програванням анімації до потрібного стану.
**Причини:**

- Анімація завжди програється повністю (детермінована поведінка)
- Глибокі посилання працюють
- Передбачувана поведінка скролу після кліку
- Реалізація через `gsap.to(timeline, { duration: 0.3, totalProgress: ... })`

**Статус (Крок 4):** частина «секція» реалізована в `04-navigation.js` (делегування + резолв номер/імʼя). Частина «крок» (`data-target-step` + авто-догравання) — на Кроці 5, коли зʼявляться покрокові таймлайни.

### ADR-004: Mobile landscape — попап замість адаптації

**Рішення:** на mobile landscape показуємо overlay «поверніть пристрій».
**Причини:**

- Контент покрокових анімацій розрахований на висоту 100vh portrait (~600-800px)
- В landscape висота ~375px — анімації не вміщуються
- Адаптація під landscape mobile = подвоєння роботи над контентом
- Аудиторія B2B-лендингу частіше в portrait

**Реалізація (`06-responsive.js`, запечено 21.05.2026):** детект **по орієнтації, а не лише ширині** — `matchMedia('(orientation: landscape) and (max-height: <config.landscapeMaxHeight=500>px) and (pointer: coarse)')`. Три умови разом: `landscape` (горизонтально) + низька висота (≤500 = телефон; планшет у landscape має 768+) + `pointer: coarse` (тач-девайс, тож десктоп із коротким вікном НЕ ловить попап). Так попап спрацьовує на **будь-якому телефоні в landscape незалежно від ширини** (вирішує проблему широких телефонів ~932px, що по ширині падали б у «таблет»).

- **Розмітка:** обгортка попапа — `[data-kulbit-landscape-popup]`, у Webflow `Display: None` за замовчуванням (показ керує JS, без блимання), fixed overlay.
- **На landscape:** попап `display:flex` + `app.observer.disable()` (fullpage off) + пауза/мут усіх відео (`rec.hide()`).
- **Назад у портрет:** попап `display:none` + `app.observer.enable()` + `updateVideoVisibility()`. Якщо сторінку **відкрили одразу в landscape** (hero тоді не побудувався, бо брейкпоінт визначається при init) — на повернення в портрет добудовуємо hero (`registerAnimations()`, з guard `!isTabletHero && !isAnimated`).
- Початковий `apply()` — через `setTimeout(…, 0)`, щоб `08-video.js` встиг заповнити `app.videos`.
- Перевірено в DevTools (device-toolbar + телефон, поворот landscape↔portrait).

### ADR-005: GitHub + jsDelivr для зберігання коду

**Рішення:** код у GitHub-репо `kulbit-webflow`, доставка через jsDelivr CDN.
**Причини:**

- Безкоштовно
- Версіонування через git
- Швидкий CDN з широкою географією
- Стандарт у Webflow-спільноті
- Сумісно з покрокового workflow (push → purge → перевірка)

### ADR-006: Модульна структура src/\* + build-конкатенація

**Рішення:** код розділений по логічних модулях з префіксами `01-`, `02-`, простий Node-скрипт склеює в один dist-файл.
**Причини:**

- Один HTTP-запит у браузері (швидше)
- Контрольований порядок виконання (через сортування за іменем)
- Простота — не треба webpack/vite/rollup для маленького проєкту
- Roman може редагувати один файл не зачіпаючи інші

### ADR-007: Декаплінг JS-гачка від стильового класу через data-атрибут

**Рішення:** елементи-снап-секції (8 секцій + footer) позначаються окремим data-атрибутом `data-kulbit-section`, header — `data-kulbit-header`. Стильові класи лишаються чистими (`.section`, `.footer`, `.header`). JS реєструє секції через `querySelectorAll('[data-kulbit-section]')`, а **не** через клас. (Обрано варіант A замість generic-класів чи `kulbit-`-префікса.)
**Причини:**

- Клас, за який чіпляється JS, перетворюється на «контракт» — generic-імена (`section`/`header`) найлегше випадково продублювати (копіюванням компонента, CMS, правками клієнта) і зламати fullpage у важкий для дебагу спосіб. Data-атрибут — явний маркер «це снап-зупинка», який ніхто не чіпатиме випадково.
- Стильові класи можна вільно перейменовувати/рефакторити, не зачіпаючи логіку (розв'язка стилю і поведінки).
- Узгоджено з уже наявним механізмом `data-target-section` / `data-target-step` (розділ 4) — data-атрибути = наш єдиний спосіб говорити з JS.
- Підсилює reorder-safe принцип: JS не залежить ні від стильових класів, ні від ручної нумерації (`data-section-index` все одно проставляється з DOM-порядку на Кроці 3).

### ADR-008: Жорсткий fullpage на Observer + transform (без ScrollSmoother)

> **Оновлено ADR-010:** модель *руху* змінено — замість лінійного зсуву треку (`y: -offsetTop`) тепер **стекінг секцій** (накладання). Observer + анти-інерція + фіксований вьюпорт із ADR-008 лишаються чинними.

**Рішення:** прибрати ScrollSmoother. Сторінка взагалі не скролиться: `#smooth-wrapper` фіксований (`position: fixed; overflow: hidden`), а `#smooth-content` (трек із 9 секцій) рухаємо трансформом `gsap.to(content, { y: -section.offsetTop })`. Навігацію драйвить GSAP **Observer** (`preventDefault: true`). Один жест = один перехід.
**Причини:**

- ScrollSmoother створений для плавного **вільного** скролу — це пряма протилежність вимозі «жодних вільних скролів, дуже жорстко». На практиці ScrollSmoother (його `normalizeScroll`) перехоплював колесо/тач паралельно з нашим Observer → секції «летіли в кінець і назад». Два механізми билися за керування скролом.
- На сайті без вільного скролу parallax «по скролу» (`data-speed`/`data-lag` — головна цінність ScrollSmoother) **не має сенсу**. Усі ефекти й так житимуть у GSAP-таймлайнах (переходи + покрокові анімації).
- `overflow: hidden` на вьюпорті = вільний скрол **фізично неможливий** (гарантія, а не прапорець).

**Анти-інерція тачпада (ключова деталь):** один фізичний свайп тачпада = довгий потік wheel-подій з інерцією. Таймер-замок це не лікує (інерція триває довше). Рішення: відрізняємо **новий флік** від **хвоста інерції** за швидкістю (`Observer.velocityY`). Хвіст сповільнюється → ігноруємо; новий флік дає сплеск швидкості (> `config.accelRatio` × попередня і > `config.minVelocity`) → приймаємо. Працює однаково на миші й тачпаді. Числа — у `KulbitApp.config`, підбирались емпірично (`accelRatio: 1.4`, `minVelocity: 60`).

**Напрямок скролу — за ТИПОМ ПОДІЇ (фінальне рішення 22.05.2026, `cb4a5a4`):** напрям визначається з `self.event.type`, а **НЕ** за девайсом. **Колесо** (`wheel`) — натурально: `onDown → +1` (вниз = далі), `onUp → -1`. **Тач** (свайп) — інверсно: `onDown → -1`, `onUp → +1` (свайп знизу-вгору = далі, як нативний скрол; GSAP шле для тача `onUp` на свайп угору). Хелпер `gestureDir(down, self) = isWheel ? (down?1:-1) : (down?-1:1)`.
> **Чому НЕ за девайсом:** спершу робили `touchInvert = matchMedia('(pointer: coarse)').matches` (21.05.2026) — але деякі десктопи **хибно звітують `pointer: coarse`** (напр. з тач-монітором), і тоді тач-інверсія ламала колесо (скрол навпаки). Діагностика підтвердила: на десктопі колесо-вниз шле `onDown event=wheel` (норма), а інвертував саме прапорець. Перехід на детект за подією це усуває й не залежить від (не)точності coarse/fine.

**Тип вводу `'wheel,touch'` (без `'pointer'`):** `'pointer'` НЕ слухаємо взагалі — щоб **затиснута ЛКМ + рух мишею** не сприймались як скрол (на будь-якому девайсі). `'wheel'` покриває десктоп (колесо/тачпад), `'touch'` — свайпи на тач-екранах. (Раніше `66700fe` робив це через девайс-прапорець — теж замінено на подієвий підхід.)

### ADR-009: Кастомні крокові анімації — керовані атрибутами (data-driven таймлайни)

**Рішення:** кастомні анімації кроків секцій описуються **data-атрибутами на самих елементах**, а JS (`buildSectionTimeline` у `03-sections.js`) збирає з них один паузований GSAP-таймлайн. Жодних значень анімації (дистанції, скейли, прозорість, послідовність) у JS-коді — усе крутиться з Webflow.

**Атрибути:**

| Атрибут | Дія (стан кроку 1) |
| --- | --- |
| `data-kulbit-y="<%>"` | цільовий `yPercent` (`-100` вгору / `100` вниз; беремо «з запасом», напр. `300`/`400`, щоб гарантовано вийти за екран) |
| `data-kulbit-scale="<n>"` | цільовий `scale` |
| `data-kulbit-scale-from="<n>"` | стартовий `scale` (крок 0; де ≠ 1) |
| `data-kulbit-fade="<n>"` | цільовий `autoAlpha` (зазвичай `0` — гарантоване зникнення) |
| `data-kulbit-order="<0\|1\|…>"` | фаза: `0` (дефолт) перша, далі — послідовно (без order — усе одночасно) |

**Механіка:** елементи прив'язуються до секції через `closest('[data-kulbit-section]')`; ті, що поза секціями (напр. `header`), йдуть до hero (секції 0). Крок 0 = стартовий стан (`gsap.set`), крок 1 = кінець таймлайну. `advance(dir)` грає/реверсить таймлайн; вхід у секцію знизу → таймлайн у кінці (щоб відмотувати). Інтегровано в snap: один жест = один крок, як у reveal-кроків.

**Тільки desktop (≥992px):** `registerAnimations` будує таймлайни лише на desktop — на таблеті/мобілці буде інший hero (Крок 8). Нижче breakpoint елементи лишаються у природному стані.

**Причини:**

- Швидкі правки й калібрування швидкостей/дистанцій без дотику до JS (Roman міняє атрибути в Webflow → publish → working).
- Узгоджено з ADR-007 (data-атрибути = єдиний спосіб говорити з JS) і reorder-safe принципом.
- Reusable: будь-яка секція стає анімованою, щойно на її елементах з'являються ці атрибути — без хардкоду «секція N = анімована».

**Vimeo bg-відео (супутнє, `08-video.js`):** **рішення — відео ЗАВЖДИ вставляється як `<iframe>` у Webflow Embed** (НЕ створюється JS-ом з атрибутів). Контейнер — `[data-kulbit-video]`, усередині — `<iframe>` Vimeo; у `src` обов'язково `&background=1` (autoplay+loop+muted+без UI), unlisted → `?h=<хеш>`. Модуль прикріплюється `new Vimeo.Player(iframe)` (для звуку), за потреби витягує iframe зі стандартної padding-обгортки (56.25%) у контейнер, і рахує **cover від контейнера** (тримається під scale-анімацією; обгортка `56.25%` дала б letterbox). Кнопка звуку — `[data-kulbit-sound]` у тій же секції (старт muted, клік → `setMuted(false)`, перемикання іконок `.icon-24-24-16` ↔ `.icon-24-24-16.is-mute`). Чому iframe, а не JS-створення: Roman керує відео візуально в Webflow, воно одразу в HTML, менше розмітки-конфігу. Скейл відео робить рушій таймлайну (ADR-009), не відео-модуль.

### ADR-010: Стекінг секцій (накладання) замість лінійного зсуву треку

**Рішення:** секції **накладаються одна на одну** (стекінг), а не йдуть лінійним треком. Це було в задумі від початку (§4 / ADR-001 «секція 2 наїжджає на секцію 1»); на пивоті ADR-008 ми тимчасово зробили лінійний зсув — тепер замінюємо на справжній стекінг. **Глобально, на всіх девайсах.**

**Механіка:**
- Кожна `[data-kulbit-section]` — `position: absolute; top:0; left:0; width:100%; height:100vh`, `z-index = індекс` (наступна вище). Контейнер `#smooth-content` — `position: relative; height: 100vh`.
- Початок: видима лише поточна і ті, що під нею (`yPercent: 0`); решта — під екраном (`yPercent: 100`).
- **Скрол вниз (N→N+1):** секція N+1 наповзає знизу (`yPercent 100→0`) поверх N, яка лишається на місці.
- **Скрол вгору (N→N-1):** секція N сповзає вниз (`0→100`), відкриваючи N-1.
- Інтегровано зі snap і кроками: спершу кроки секції (reveal/таймлайн), потім стекінг-перехід. Вхід знизу → кроки/таймлайн секції у кінці (щоб відмотувати).

**Абсолют — лише в рантаймі (важливо для Webflow):** `setupStacking` перебиває позиціонування JS-ом на проді. У Webflow Designer (Footer-код не виконується) секції лишаються `position: relative; top:0; height:100vh; overflow:hidden` — контент зручно редагувати. Геометрію тримає код, не залежить від Webflow-CSS.

**Чому так, а не трек:** `yPercent` (відносно висоти) + `height:100vh` роблять позиції **resize-proof** — нічого не перераховувати по `offsetTop` (на відміну від ADR-008). `handleResize` лише перевиставляє дискретні позиції.

**Реалізація:** `setupStacking` / `applyStackingPositions` + переписані `goToSection` і `goToSectionStep` (`03-sections.js`); виклик `setupStacking` в `init` (`02-app-core.js`). Замінює модель руху з ADR-008 (трек фіксований, рухаються секції). Перевірено консольним тестом.

### ADR-011: Брейкпоінт-залежний таблет/мобайл-hero + пауза/звук відео по видимості

**Рішення:** окрема **hero-хореографія** (`buildTabletHero`), що сплітається зі стекінгом наступної секції, спільна для **таблета (768-991px) і мобілки-портрет (≤479px)**; та **загальна пауза/мут відео**, коли воно накрите.

**Брейкпоінт hero** визначається ОДНОРАЗОВО при `init` (`registerAnimations`, межі = стандартні брейкпоінти Webflow): `≥992` → desktop-таймлайни (`buildDesktopAnimations`, ADR-009), `768-991` **і** `≤479` → `buildTabletHero` (та сама хореографія), `480-767` → нічого (діапазон попапа landscape, Крок 8). Зміна брейкпоінта потребує reload (динамічний `gsap.matchMedia` — на Крок 8, щоб не ускладнювати зараз). Атрибути — з суфіксом `-tablet` (`data-kulbit-y-tablet` тощо), живуть на тих самих елементах, що й десктопні; **мобілка читає ті самі `-tablet` атрибути** (анімація ідентична), нових `-mobile` атрибутів немає. Геометрія (16:9-висота, центр кнопки, `partial`-накриття) рахується динамічно від поточного екрана → сама адаптується під вузький портрет.

> **Чому мобілка = таблет (рішення 21.05.2026):** Roman підтвердив, що анімація на мобілці **точно така ж**, як на таблеті. Тому переюзаємо `buildTabletHero`/`tabletHeroStep` як є (одна гілка в диспетчері), без дублювання коду і без нових атрибутів. На вузькому портреті 16:9-смуга виходить невисокою й приклеєна до верху — це той самий принцип, інші пропорції екрана. Перевірено консольним тестом на ≤479 (всі 3 кроки + відмотування + пауза/звук). Якщо колись мобілка має розійтись із таблетом — додаємо `-mobile` атрибути й окрему гілку.

**Таблет-hero — 3 кроки** (бесп. таймлайн із мітками s1/s2/s3, на відміну від простого 1-крокового десктопного):
- крок 1: `-tablet` атрибути — `.flex-h-v-v.is-hero` весь блок вниз+згас, header вгору, відео `scale 1.3→1`; **+ кнопка `.hero-video-button` центрується у видимий екран** (на ≤991 кнопка має CSS `top:25vh`, тож її явно зсуваємо в центр `#smooth-wrapper.clientHeight/2`);
- крок 2: `.hero-video` height → 16:9 (cover сам перефітовує iframe) + секція 2 наповзає під 16:9 (`yPercent 100→partial`) + кнопка зсувається в центр 16:9 (вимір через `getBoundingClientRect`);

> **⚠️ Рендер-висота, а не `innerHeight` (фікс 21.05.2026):** `partial` рахується від `section2.offsetHeight`, а центр кнопки — від `#smooth-wrapper.clientHeight`, **НЕ від `window.innerHeight`**. На мобілці адресний бар робить `100vh (CSS) ≠ innerHeight (JS)`: якщо рахувати зсув секції 2 від `innerHeight`, а секція має `height:100vh` — між 16:9-відео і секцією 2 зʼявляється смуга (видно фон сайту). Беремо рендер-висоти самих елементів → зсув точний на будь-якому девайсі. На десктоп-тулбарі баг не відтворюється (`100vh == innerHeight`) — перевіряти на реальному телефоні.
- крок 3: секція 2 `partial→0` (повне накриття = перехід на секцію 1).

**Межа hero↔секція1 (`tabletHeroStep`):** на hero (idx 0) крокуємо таймлайн (0↔3); досягли кроку 3 → передаємо керування (`currentSectionIndex=1`); назад із секції 1 (idx 1, dir<0) — **відмотуємо хореографію** (секція 2 → partial, відео → повний екран, контент назад), а не звичайний стекінг. Це ключова відмінність від стандартного переходу. `advance` делегує на `tabletHeroStep` першим.

**Пауза/звук по видимості (`08-video.js`, загальне):** кожне відео грає лише коли його секція поточна (`sectionIndex === currentSectionIndex`). Накрилось → `pause()` + мут; повернулось → `play()` + звук як був (намір `soundOn`, перемикає кнопка). **Таймінг (уточнено — ADR-012):** показати поточне відео — ОДРАЗУ (`showCurrentVideo`, грає поки секцію наповзає/відкривають); сховати накриті — на ЗАВЕРШЕННІ переходу (`hideOtherVideos` в `onComplete` `goToSection` — коли секція торкнулась верху екрана), а не на початку наповзання. На таблеті/мобілці пауза вже була в `onComplete` кроку 3 (`tabletHeroStep`). Гайд `landscapeBlocked` (попап) → відео завжди на паузі в landscape.

**Реалізація:** `buildTabletHero` / `tabletHeroStep` (`03-sections.js`); `showCurrentVideo`/`hideOtherVideos`/`updateVideoVisibility` + запис `{player, sectionIndex, show, hide}` у `app.videos` (`08-video.js`). Перевірено real-scroll консольним тестом (вперед, передача, повне відмотування назад, пауза/звук).

### ADR-012: Динамічні брейкпоінти через gsap.matchMedia + таймінг паузи відео при накритті

**Рішення:** hero перебудовується при зміні ширини/орієнтації **без reload** — `registerAnimations` будує не одноразово за `innerWidth`, а реєструє `gsap.matchMedia` контексти на брейкпоінтах `992 / 768 / 479`. Скасовує обмеження «зміна брейкпоінта потребує reload» з ADR-011.

**Механіка:**
- `app.mm = gsap.matchMedia()`; контексти: `(min-width:992)` → `buildDesktopAnimations`; `(768-991)` і `(max-width:479)` → `buildTabletHero` (спільна гілка); `480-767` → нічого (попап landscape).
- **Вхід у брейкпоінт:** `resetHeroState()` (currentSectionIndex=0, currentStep=0, `applyStackingPositions`, `updateVideoVisibility`) → потім `build*`. Кожен брейкпоінт стартує з **чистого стану на hero**.
- **Вихід:** GSAP сам ревертить усі таймлайни/сети, створені в контексті (бо `build*` кличуться всередині `matchMedia`-колбеку = `gsap.context`); `teardownHero()` лише обнуляє наші посилання/прапорці (`timeline/isAnimated/tabletTL/isTabletHero`).
- **Trade-off (свідомий):** на зміні брейкпоінта повертаємось на hero (а не зберігаємо позицію) — так уникаємо складної реконсиляції стану таблет-хореографії. Реальний кейс — поворот девайса/ресайз, де старт із топу прийнятний.
- **Інтеграція з попапом landscape:** `06-responsive.js` ставить `app.landscapeBlocked` (поважає логіка відео) і **більше не добудовує hero вручну** — це робить matchMedia (поворот телефона портрет↔landscape перетинає брейкпоінти 479/768 → авто-перебудова).

**Таймінг паузи відео (друга частина ADR):** пауза відео тепер на ЗАВЕРШЕННІ накриття (секція торкнулась верху екрана), а не на початку наповзання — див. блок «Пауза/звук» вище (`showCurrentVideo`/`hideOtherVideos`).

**Реалізація:** `registerAnimations` (matchMedia) + `resetHeroState` + `teardownHero` (`03-sections.js`); `app.mm`/`app.landscapeBlocked` у стані; розділ `showCurrentVideo`/`hideOtherVideos` (`08-video.js`). ✅ Перевірено ресайзом на staging (desktop↔tablet↔mobile без reload) + таймінг паузи відео + попап landscape — Roman підтвердив 21.05.2026.

### ADR-013: Покрокова хореографія секції is-our-clients (десктоп) — section.oc + ScrambleText

**Рішення:** друга реальна анімована секція (`.section.is-our-clients`) — **власна покрокова хореографія**, інкапсульована в `section.oc`, інтегрована в snap (як `tabletHeroStep`). 3 окремі анімації планувались per-breakpoint; **наразі зроблено ДЕСКТОП** (tablet/mobile — далі, лейаут інший).

**Десктоп-механіка (3 стани = 3 набори карток `.our-client-card-wrapper`):**
- **Поява секції** (`oc.enter`, вхід згори): прогрес-бар наповнюється `0→1/3`; набір 1; `H2 .text-size-section-h2` + `.text-size-section-label` **скрамбляться** (тривалість 1.2с).
- **Крок 1** (`oc.step`): прогрес `1/3→2/3`, набір 1 їде вгору (`yPercent -100`), набір 2 наповзає знизу.
- **Крок 2**: прогрес `2/3→3/3`, набір 3 наповзає, **обидва параграфи переписуються** (скрамбл: стерти → написати новий текст).
- **Наступний скрол** після набору 3 → наступна секція (стекінг). Реверс назад: вхід знизу (`oc.reset(true)`) ставить кінцевий стан (набір 3, прогрес повний, тексти next), скроли вгору відмотують.

**Прогрес-бар:** трек `.section-progresbar` → `#fdfcfc @ 15%`; **дочірня лінія-заповнення** (`.oc-fill`, створює JS) → `#fdfcfc @ 100%`, ширина по третинах `(state+1)/3`.

**Скрамбл зі збереженням спанів (ключова деталь):** ScrambleText затирає внутрішні спани при перезаписі цілого елемента, тому **скрамблимо кожен сегмент (span) окремо**; пробіл між спанами — **статичний текст-вузол** (ScrambleText обрізає крайні пробіли → інакше «atscale»). Для H2/label (текст не міняється) — після скрамблу **відновлюємо точний оригінальний `innerHTML`** (спани/кольори лишаються як у верстці). Обгортки параграфів отримують **фіксовану висоту** (абсолютний елемент `.icon-30-24-24.is-our-clients` вирівняний по низу контейнера — без фіксації він з'їжджав під час стирання).

**Таблет/мобілка-механіка (`buildOurClients('tablet'|'mobile')`):** набори — вертикальні колонки. Та сама логіка по наборах + **окремий крок «зсув карток»** на наборах із зсувом. Зсуви per-набір задає **`shiftN`**: **таблет `[1,0,1]`** (набір 1/3 на 1, набір 2 без зсуву), **мобілка `[2,1,2]`** (набір 1/3 на 2, набір 2 на 1). `STAGES`-машина **генерується з `shiftN`**: поява → (зсув набору i, якщо `shiftN[i]>0`) → свап до наступного → … Зсув = переклад карток усередині врапера на `cardStep` (відстань між сусідніми картками, `getBoundingClientRect`). Прогрес — по наборах (`d.cur`); скрамбл текстів — на стадії появи останнього набору (`scrIdx`). ⚠️ Потребує **вертикальної колонки карток** (а не wrap-сітки), щоб `cardStep` був вертикальний.

**Фікс H2 (стабільна висота при скрамблі):** випадкові символи скрамблу в середньому ширші за фінальні (а H2 — `justify` + `uppercase`), тож текст тимчасово переносився на зайвий рядок (4↔5). Рішення: `scrambleIn` на час скрамблу ставить елементу `height: <зафіксована>` + `overflow: hidden` → зайвий рядок обрізається, висота стабільна (на завершенні відновлюємо точний HTML).

**Поява/таймінг (фікси `d236286` → `b022ae2`):** під час наїзду секції заголовки приховані (`autoAlpha:0`), прогрес 0%; механіка `oc.prepare()` (наїзд) → `oc.enter()` (повне накриття → прогрес 0→1/3). Десктоп: `goToSection` кличе `prepare` на старті слайду, `enter` в `onComplete`.
- **Скрамбл заголовків — КЕРУЄТЬСЯ ВИДИМІСТЮ У ВЬЮПОРТІ** (`15422ae`, незалежно від кроків OC): персистентний `IntersectionObserver` на H2/label (пороги `[0, 0.6]`) → поява у вьюпорті (`ratio≥0.6`) = скрамбл-**IN** (порожньо→текст); повний вихід (`!isIntersecting`) = скрамбл-**OUT** (текст→порожньо); знову поява = знову IN. **Не реагує на просте накриття сусідньою секцією** (лише на реальний вихід із вьюпорта) — тому повернення без зайвого повторного скрамблу. Кожен заголовок — `makeHeadCtrl` (сегментовані спани зі збереженими кольорами, висота зафіксована + `overflow:hidden`). IO незалежний від `enter`/`prepare`; прибирається в `teardownHero` (`oc.dispose`). (Виняток — перепис параграфів на набір 3, навмисне посеред скролу.)
- **Прогрес назад — ПЛАВНО** (`b022ae2`): при реверсі `prepareCommon` зменшує лінію через `gsap.to(0%)` (раніше `gsap.set` — різко).

**Таблет ↔ hero (фікс `d236286`):** секція 1 = is-our-clients, але її «вмикає» таблет-hero (наповзає під 16:9). Тому: hero-передача (`tabletHeroStep` крок 3) кличе `s1.oc.enter()` (раніше обходила `goToSection` → OC не ініціалізувався, при скролі назад повертався 16:9). Реверс «назад із секції 1»: `tabletHeroStep` **відкладається на OC поки `oc.state>0`** (повертає false → `advance` кличе `oc.step(-1)`); і лише коли OC на старті — реверсить hero (+ `oc.prepare()` ховає заголовки/прогрес для наступної появи).

**Реалізація:** `buildOurClients(mode)` (`03-sections.js`, `mode` = `'desktop'`/`'tablet'`/`'mobile'`; tablet+mobile ділять одну параметризовану гілку (різниця — `shiftN`); спільні скрамбл/прогрес/текст-утиліти) → `section.oc = { prepare, enter, reset, step, state, dispose }`; `advance`/`goToSection`/`tabletHeroStep` делегують; `01-init.js` реєструє `ScrambleTextPlugin`. matchMedia: desktop → `'desktop'`, tablet (768-991) → `'tablet'`, mobile (≤479) → `'mobile'`. **Усі 3 брейкпоінти зроблено й ✅ ПІДТВЕРДЖЕНО Romanом** (`e96c976` desktop, `9e1a958` tablet, `8a1b3d0` mobile; фікси `d236286`/`b022ae2`/`15422ae`). Секція is-our-clients — ПОВНІСТЮ ЗАВЕРШЕНА на всіх девайсах.

### ADR-014: Кастомний Vimeo-плеєр проєктів (10-project-video.js) — cover + власні контроли

**Рішення:** відео-портфоліо в секції `is-projects` — окремий модуль `10-project-video.js` з **повністю кастомними контролами** (рідні Vimeo-контроли вимкнено `&controls=0`). На відміну від hero-відео (`08-video.js`, фонове autoplay) — це **кероване користувачем** відео зі звуком, play/pause, перемоткою, гучністю, fullscreen.

**Розмітка-контракт (data-атрибути, ADR-007):** корінь `[data-kulbit-project-video]` (на `.projects-video-wrapper`; усередині `<iframe>` Vimeo); `[data-kulbit-poster]` (постер до старту); `[data-kulbit-play]` (велика кнопка старту); `[data-kulbit-controls]` (панель, `display:none` дефолт); у ній `[data-kulbit-toggle]` з `[data-kulbit-icon-play]`/`[data-kulbit-icon-pause]`, таймери `[data-kulbit-time-current]`/`[data-kulbit-time-total]`, доріжка `[data-kulbit-seek]` з `[data-kulbit-seek-fill]` (прогрес) + `[data-kulbit-buffer]` (завантажено), гучність `[data-kulbit-volume]` з `[data-kulbit-icon-volume]`/`[data-kulbit-icon-mute]` + `[data-kulbit-volume-popup]`>`[data-kulbit-volume-track]`>`[data-kulbit-volume-fill]`, `[data-kulbit-fullscreen]`.

**Механіка:**
- **Cover** — iframe масштабується так, щоб 16:9 покривало корінь (та сама математика, що в `08-video.js`); `ResizeObserver` + перерахунок на `fullscreenchange`.
- **Vimeo SDK** (`new Vimeo.Player(iframe)`): подія `timeupdate.percent` = зіграна частка (прогрес+час), `progress.percent` = завантажена частка (буфер).
- **Перемотка/гучність** — `pointerdown/move/up` + `setPointerCapture` (працює і за межами елемента). Доріжка горизонтальна; повзунок гучності вертикальний (верх = більше, `fill.height %`).
- **Попап гучності** — плавний (gsap `fromTo` fade + легкий під'їзд `y:8→0`, ~0.2с; `killTweensOf` + guard у `onComplete` від гонки відкр/закр); клік по динаміку відкр/закр, клік поза попапом — закрити.
- **Іконки** — `play↔pause` за реальним станом плеєра (події), `volume↔mute` за рівнем (0 = mute); перемикання через `autoAlpha`.
- **Fullscreen** — нативний API на корінь (+ webkit-fallback); контроли лишаються (вони діти кореня). iOS-обмеження: fullscreen дива не працює на iPhone Safari (guard + warn).
- **Пауза за кадром** — запис `{player, sectionIndex, show, hide}` у `app.videos`; `hide()` = `player.pause()`, `show()` = **no-op** (плей лише вручну, без автоплею). Навігація (`hideOtherVideos`) глушить звук, коли секцію накрито стекінгом або в landscape-попапі.

**Причини:** клієнт хоче брендовані контроли (не Vimeo-UI); cover потрібен для дизайну (не letterbox); pointer-події = єдина модель для миші+тача; інтеграція з `app.videos` уникає звуку off-screen без дублювання логіки.

**Реалізація:** `10-project-video.js` (новий, `initProjectVideo` на кожен `[data-kulbit-project-video]`). ✅ Перевірено покроково в консолі (cover, play/pause, таймери, доріжка+буфер, гучність-попап, fullscreen) — Roman підтвердив. Коміт `da2ac68`.
**Фікс (`f0928a9`):** `pointer-events: none` на projects-iframe — Vimeo-iframe (cross-origin) перехоплював wheel, тож снап не реагував, коли курсор над відео. Контроли власні (поверх), тож iframe-у події миші не потрібні → колесо проходить наскрізь до Observer. Ставиться в JS (авто для всіх відео, зокрема майбутніх клієнтських) — не інлайном на embed.

### ADR-015: Свап-секція is-projects (вертикальний свап відео + END-картинка) в snap — section.pv

**Рішення:** секція `is-projects` — **адаптивна** покрокова хореографія: вертикальний свап відео (поточне `height 100→0`, наступне виростає знизу `0→100`), фінальна **END-картинка**, інтегровано в snap як `section.pv` (повний аналог `section.oc`, ADR-013). **Поки лише DESKTOP** (таблет/мобілка — інша анімація відео, згодом — підтверджено Romanом).

**Адаптивність (ключове):** елементи свапу = **прямі діти `[data-kulbit-project-group]`** (на `.projects-cards-wrapper`); JS рахує їх із DOM і будує `N−1` свапів автоматично. Клієнт додає відео — анімація сама підхоплює, **нічого не хардкодимо** (reorder/add-safe, ADR-007 / [[sections-reorder-safe]]).

**Розмітка:** група `[data-kulbit-project-group]`; відео = блоки `[data-kulbit-project-video]` (повний плеєр, ADR-014); **остання** дитина — `[data-kulbit-project-end]` (картинка). END як останній елемент: коли він активний і скрол далі → перехід на наступну секцію.

**Механіка свапу:** активний елемент `height 100%`, решта `0`; усі елементи зсунуті по `y` на `-(індекс × гэп)` — CSS-гэп лишається між відео (видно **під час переходу**), але над активним ховається за верхній край (немає порожньої смуги зверху); `overflow:hidden` на групі. Згорнуте відео **скидається на постер** (пауза, `currentTime=0`, контроли off) — `resetItem`.

**Прогрес-бар (спільна логіка з ADR-013, ВИПРАВЛЕНА):** трек `.section-progresbar` (15% білого) + дочірня лінія `.pv-fill` (100%), ширина `(state+1)/total`. Тайминг лінії: **миттєво 0** при `prepare` (наїзд), **0→1/N** на `enter` (повне накриття/поява), **плавно→0** на `collapse` (секція сповзає геть).

**Фікс бага прогрес-лінії is-our-clients (і pv):** раніше при сповзанні секції лінія не спорожнювалась (зависала повна), а при повторній появі йшла назад→вперед (видимий глюк). Тепер: `collapse()` спорожнює лінію разом зі сповзанням (у `goToSection` dir<0 для секції-`leaving`); `prepareCommon` миттєвий (`gsap.set`, не `gsap.to`) — без зворотного руху під час появи. Застосовано і до `oc`, і до `pv`.

**Реалізація:** `buildProjects(mode)` + `section.pv = { state, prepare, enter, collapse, reset, step, dispose }` (`03-sections.js`); `advance`/`goToSection` делегують (паралельно `isOurClients`); `collapse()` додано в `oc`; `dispose()` повертає натуральний лейаут на вихід із desktop-брейкпоінта (висоти/трансформ зі `step` живуть поза matchMedia-контекстом). matchMedia: лише `≥992` → `buildProjects('desktop')`. ✅ Перевірено реальним скролом на staging — Roman підтвердив (свап, гэп+зсув, прогрес, скидання, фікс лінії, скрол над відео). Коміти `7c6a1eb` (свап+фікс лінії), `f0928a9` (pointer-events). **Це поточний стан коду (стабільний, desktop-only для is-projects).**

### ADR-016: is-projects на tablet/mobile (вікно 3) + персистентність позиції + landscape/fullscreen + UX

> 🔄 **ЧАСТКОВО ПОВЕРНЕНО 26.05.2026.** Початковий батч усього ADR-016 був відкочений (`3978c4d`) через велику кількість проблем + регресію скролу. Тепер повертаємо по ОДНІЙ фічі з тестом:
> - **Пункт 1 (tablet/mobile свап вікном 3) — ✅ У КОДІ** (`a34408b` свап + `86b1c33` фікс slotH). Підтверджено на desktop + iPhone Safari. Деталі — у п.1 нижче.
> - **Пункт 5 (персистентність позиції, sessionStorage) — ✅ У КОДІ** (`8e932f8` + `a0d1a7c`). `persistSection` у `goToSection` ТА в `tabletHeroStep` (hero↔секція1 іде в обхід goToSection!); `restoreSection` у кінці matchMedia-гілок приводить hero-таймлайн у кінець (хедер схований ШТАТНО, не `gsap.set`) + миттєвий `goToSection(saved,true,1)`. Рівень — секція (внутрішній крок з початку).
> - **Контролі відео tablet/mobile (≤991) — ✅ У КОДІ** (`64225c8`..`f032eab`): fullscreen через **Vimeo SDK** `player.requestFullscreen()` (нативний iOS відео-fullscreen — на `<div>` API не працює); кнопка звуку = **mute-toggle** (`setMuted`, бо рівень гучності iOS ігнорує); перший клік play лише стартує (звук — окремою кнопкою: на iOS розмутити можна ЛИШЕ відео, що ВЖЕ грає, синхронно в gesture, як hero `background=1`); desktop fullscreen блокує навігацію (`observer.disable/enable`).
> - **Пункти 2–4, 6 («одне відео»), 7 (auto-landscape fullscreen)** — **ще НЕ повернено**; опис нижче = специфікація. Історія: git `50fb693`..`1500d68`.

**Рішення (одна сесія, 26.05.2026):** доробка is-projects на менші екрани + наскрізні UX-фічі навігації.

**1. Tablet/mobile свап — ВІКНО з 3 блоків (`buildProjects` уніфіковано через `WIN`):** desktop `WIN=1` (одне відео на повну), tablet/mobile `WIN=Math.min(3, total)` (видно 3 блоки). **Спільна машина станів:** `state` = старт вікна `[state..state+WIN-1]`; `maxState = total - WIN` (останній стан — коли END став нижнім видимим → далі `advance` до сусідньої секції). Свап: верхній елемент вікна схлопується (`height→0`), знизу виростає наступний; уся колонка зсунута по `y` на `-(state*гэп)`. **Висота блоків** — від `#smooth-wrapper` (видимий вьюпорт) мінус простір над групою мінус `padding-bottom` секції. **`slotH = (availH - (WIN-1)*гэп)/WIN`**.
>
> ✅ **Фактична реалізація (`a34408b` + `86b1c33`) — 2 критичні нюанси, виявлені на тесті:**
> - **`flex: none` обов'язковий** перед керуванням `height`: картки в розмітці мають `flex: 1 1 0%`, тож `flex-grow` ділить висоту порівну й `height` повністю ІГНОРУЄТЬСЯ (бачив усі 4 рівні замість 3). `gsap.set(els, {flex:'none'})` раз на побудові — і `height` починає діяти. Desktop лишає `height` у `%` (там CSS картки інші, реагують). `dispose` чистить `clearProps: 'height,transform,flex'`.
> - **`slotH` рахується ВІДНОСНО СЕКЦІЇ, не вьюпорта:** `gTop = group.top - section.el.top` (а не `group.getBoundingClientRect().top`). Інакше при наїзді (секція ще `yPercent 100`, під екраном) `slotH` виходив невірним → стрибок стану на завершенні появи. Відносно секції — однаковий незалежно від позиції стекінгу. (Десктоп імунний — там `%`.)

**2. Динамічна висота:** `onResize` → `applyState(state, false)` перевиставляє висоти/зсув (зміна висоти девайса/інспектора підтягує блоки). Слухач знімається в `dispose`.

**3. Поява (tablet/mobile, на `enter`):** блоки вікна по черзі виїздять знизу (`y: RISE→0`) + `opacity` (gsap `stagger`). `prepare` ховає їх (`autoAlpha 0`) під час наїзду секції, `enter` (повне накриття) програє появу. Desktop появи блоків не має (лише прогрес).

**4. Прогрес-бар СКРІЗЬ:** ширина `(state+1)/(maxState+1)` на всіх брейкпоінтах (раніше лише desktop).

**5. Персистентність позиції (`sessionStorage`, ADR-008a у коді):** `persistSection` зберігає `currentSectionIndex` у `goToSection`; `restoreSection` (в кінці кожної matchMedia-гілки, після `build*`) робить миттєвий `goToSection(saved, true, 1)`. Reload і зміна брейкпоінта (поворот девайса) більше НЕ скидають на hero. **Реверсує trade-off ADR-012.** Рівень — секція (внутрішній крок стартує з початку). При відновленні на НЕ-першу секцію `restoreSection` ховає `[data-kulbit-header]` (`autoAlpha 0`) — інакше hero-таймлайн на кроці 0 лишав би хедер видимим над секцією; керування хедером далі в hero-анімації (reverse при поверненні вгору).

**6. Грає лише ОДНЕ відео (`10-project-video.js`):** реєстр усіх projects-плеєрів; на подію `play` `pauseOthers` скидає решту на постер (пауза + `currentTime 0` + thumb). Актуально для tablet/mobile, де у вікні видно кілька відео.

**7. Fullscreen у landscape + попап (`10-project-video.js` + `06-responsive.js`):** на вхід у fullscreen — `screen.orientation.lock('landscape')` (мобілка/таблет відкривають відео горизонтально; десктоп ігнорує через `catch`), на вихід (Esc/back/кнопка) — `unlock` (через `fullscreenchange`). Попап «поверни телефон» (`06-responsive.js`) **поважає fullscreen**: поки `document.fullscreenElement` активний — попап OFF, навігація OFF, відео грає (інакше landscape-фуллскрін одразу тригерив би попап).

**Реалізація:** `buildProjects` (уніфіковано, `03-sections.js`); `persistSection`/`restoreSection` (`03-sections.js`); `registry`/`pauseOthers`/`lockLandscape`/`unlockOrientation` (`10-project-video.js`); `inFullscreen`-guard + `fullscreenchange` (`06-responsive.js`). Коміти: `50fb693` (персистентність), `e47a8d7` (хедер), `2a096a9` (одне відео), `2d438a7` (tablet/mobile свап+прогрес+resize), `41789ff` (landscape/fullscreen). ⏳ **Очікує підтвердження на реальному девайсі** (tablet/mobile свап, поява, landscape-fullscreen, поворот).

---

